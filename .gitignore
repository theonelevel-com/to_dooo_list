.DS_Store

# clasp — automated Apps Script deployment (private, personalised; keep out of public repo)
.clasp.json
.clasprc.json
clasp-deploy/

# editor / session tooling
.claude/

# local scratch screenshots (not part of the repo).
# The user's dated grabs are named "To Do v*"; the one canonical tracked
# screenshot is "to dooo v1.1.png", so this prefix glob leaves it alone.
example/To Do *
.wrangler/
